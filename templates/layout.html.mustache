<!DOCTYPE html>
<html lang="en" class="bg-zinc-800">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="stylesheet" href="{{conf.TegakiCssUrl}}">
		<link rel="stylesheet" href="{{conf.TailwindCssUrl}}"/>
		<link rel="stylesheet" href="{{conf.CssUrl}}"/>
		<link rel="icon" href="{{conf.FaviconUrl}}"/>
		<title>
			Eientei Archive
			{{#board}}
				- {{board}}
			{{/board}}
		</title>
		<script src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.6.1/MathJax.js?config=TeX-AMS_HTML-full"></script>
		<script src="{{conf.TegakiJsUrl}}"></script>
	</head>
	<body>
		<div class="bg-zinc-900 w-full px-4 lg:px-8 py-4">
			<div class="flex-row gap-3 hidden lg:flex text-violet-500">
				Boards:
				{{#boards}}
					<a href="/{{Name}}" class="hover:text-violet-600">/{{Name}}/</a>
				{{/boards}}
			</div>
			<div class="flex flex-col justify-between md:flex-row lg:mt-2 gap-3">
				<div class="flex flex-row gap-3">
					<a class="text-3xl font-bold text-slate-200" href="/">Eientei</a>
					<a class="text-2xl font-bold text-slate-200 self-end" href="/contact">Contact</a>
				</div>
				{{#board}}
					<form action="/{{board}}/search">
						<div class="flex flex-col md:flex-row gap-2">
							<input
								type="text"
								name="search"
								class="px-3 py-1 text-slate-900 rounded"
								placeholder="Search"
								{{#search}}
									value="{{search}}"
								{{/search}}
							/>
							<button class="bg-violet-500 hover:bg-violet-600 px-3 py-1 rounded">🔍</button>
						</div>
					</form>
				{{/board}}
			</div>
		</div>
		<div class="w-full px-4 lg:px-8 py-4">
			{{{content}}}
		</div>
		<script src="{{conf.JsUrl}}"></script>
	</body>
</html>
